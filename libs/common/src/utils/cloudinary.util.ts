export function optimizeCloudinaryUrl(url: string): string {
    // return url.replace(
    //     `res.cloudinary.com/${process.env.CLOUDINARY_CLOUD_NAME}/image/upload`,
    //     `images.techcell.cloud/upload/f_auto,q_auto,dpr_auto`,
    // );

    return url;
}
